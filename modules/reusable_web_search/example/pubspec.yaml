name: reusable_web_search_example
description: Example app demonstrating the reusable_web_search package
publish_to: 'none'
version: 1.1.0

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  reusable_web_search:
    path: ../

  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
